spring.application.name=backend
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/projectmate}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:@rahul.sahu123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}

# CORS settings
app.cors.allowedOrigins=${FRONTEND_URL:http://localhost:5173}

# Gemini API Key (set via environment variable or default)
gemini.api.key=${GEMINI_API_KEY:AIzaSyCFVsDCgA6NSdEfDNkQukyKHJwU_3CXq7w}
